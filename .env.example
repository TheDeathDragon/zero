# GitHub API Token (可选)
# 如果不设置，将使用公开访问模式（有 API 速率限制）
# 设置后可提高 API 调用限制（60次/小时 -> 5000次/小时）
# 创建方式：GitHub Settings -> Developer settings -> Personal access tokens -> Generate new token
# 只需要 public_repo 权限即可
GITHUB_TOKEN=
